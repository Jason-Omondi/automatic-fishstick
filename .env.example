# Environment: development or production
# development: pretty-printed colored logs
# production: JSON structured logs
ENV=development

# Database Configuration
# Type: mysql or postgres
DB_TYPE=mysql

# Database Connection
# User: database username
# Password: database password (never commit real passwords - use .env locally)
# Host: database hostname (localhost for local, mysql for Docker container)
# Port: database port (3306 for MySQL, 5432 for PostgreSQL)
# Name: database name
DB_USER=root
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ecomgo

# PostgreSQL Configuration (when switching from MySQL)
# Uncomment and update if using PostgreSQL instead
# DB_TYPE=postgres
# DB_USER=postgres
# DB_PASSWORD=your_secure_password_here
# DB_HOST=localhost
# DB_PORT=5432
# DB_SSLMODE can be: disable, allow, prefer, require, verify-ca, verify-full
DB_SSLMODE=disable

# Server Configuration
# PORT: port where API server listens
SERVER_PORT=8085

# Keycloak Configuration (for future OAuth2/OpenID Connect integration)
# URL: Keycloak server URL
# REALM: Keycloak realm name
# CLIENT_ID: OAuth2 client ID
# CLIENT_SECRET: OAuth2 client secret (never commit real secret - use .env locally)
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=master
KEYCLOAK_CLIENT_ID=ecomgo
KEYCLOAK_CLIENT_SECRET=your_client_secret_here

# Note: This is an example file for reference.
# For local development:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update .env with your actual credentials
# 3. .env is in .gitignore - never commit it to version control
#
# For production:
# - Use actual secure passwords (minimum 12 characters)
# - Use strong, randomly generated secrets
# - Use environment-specific variables
# - Rotate secrets regularly
# - Use a secrets management system (e.g., HashiCorp Vault, AWS Secrets Manager)
# - Set ENV=production for JSON logging
